# Let Hibernate create/drop schema for @DataJpaTest
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Don't run Flyway during tests (your migrations don't make the tables yet)
spring.flyway.enabled=false
